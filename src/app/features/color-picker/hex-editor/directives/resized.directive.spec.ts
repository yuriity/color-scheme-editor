import { ResizedDirective } from './resized.directive';

describe('ResizedDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizedDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
