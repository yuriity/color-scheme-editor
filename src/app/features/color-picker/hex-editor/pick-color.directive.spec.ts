import { PickColorDirective } from './pick-color.directive';

describe('PickColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PickColorDirective();
    expect(directive).toBeTruthy();
  });
});
